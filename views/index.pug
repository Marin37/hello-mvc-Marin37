extends layout

block content
  h1 Hello MVC!
  p Esto es un Modelo-Vista-Controlador conectado a una DataBase en Mongo.
  br
  p Ingresá el ID del usuario que querés buscar:
  form.form-inline(action='/search' method='get')
    div.form-group.mr-sm-3
      input.form-control(type='text' name='id' placeholder='ID...')
    input.form-control.btn.btn-primary(type='submit' value='Buscar')
  hr
  table.table.table-striped
    thead.thead-dark
      th ID
      th Nombre
      th Email
      th Cumpleaños
      th DNI
      th Edad
    tbody
    each user in users
      tr
        td= user.id
        td= user.name
        td= user.mail
        td= user.birthday.toLocaleDateString('es-AR')
        td= user.dni
        td= user.age
  hr
  h4 Más info en 
    a(href='#') MVC